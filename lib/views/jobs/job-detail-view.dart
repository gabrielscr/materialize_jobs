import 'package:flutter/material.dart';

class JobDetailView extends StatelessWidget {
  final int id;

  const JobDetailView({Key key, @required this.id}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
